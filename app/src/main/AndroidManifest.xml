<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sigap.dua.empat">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".layananPolres.satResKrim.SidikJariActivity"
            android:parentActivityName=".MenuLayananPolres"></activity>
        <activity
            android:name=".MenuLayananPolres"
            android:parentActivityName=".MenuUtamaActivity" />
        <activity android:name=".MenuUtamaActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>